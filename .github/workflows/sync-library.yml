name: Sync moixa-py library

on:
  schedule:
    - cron: '0 4 * * *'
  workflow_dispatch:

jobs:
  sync:
    name: Sync from codebeetl/moixa-api
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Clone moixa-api
        run: git clone --depth 1 https://github.com/codebeetl/moixa-api.git /tmp/moixa-api

      - name: Sync library files
        run: |
          rsync -av --delete --exclude='cli.py' \
            /tmp/moixa-api/moixa_py/ \
            custom_components/moixa/moixa_py/

      - name: Check for changes
        id: changes
        run: |
          if git diff --quiet custom_components/moixa/moixa_py/; then
            echo "changed=false" >> "$GITHUB_OUTPUT"
          else
            echo "changed=true" >> "$GITHUB_OUTPUT"
          fi

      - name: Commit and push
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add custom_components/moixa/moixa_py/
          git commit -m "Sync moixa-py library from upstream"
          git push
